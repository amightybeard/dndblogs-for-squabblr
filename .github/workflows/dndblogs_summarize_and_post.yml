name: Summarization and Posting Workflow

on:
  # schedule:
    # - cron: '0 * * * *'  # Run every hour
  workflow_dispatch:  # This allows you to manually trigger the workflow from the GitHub Actions UI

jobs:
  summarize_and_post:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
    - name: Summarize and Post Article
      run: python dndblogs.py summarize_and_post_article
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SQUABBLR_TOKEN: ${{ secrets.SQUABBLR_TOKEN }}}
